apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    app: "{{ .Chart.Name }}"
    host: {{ template "host" . }}
spec:
  ports:
  - port: {{ .Values.django.port }}
  selector:
    app: "{{ .Chart.Name }}"
  sessionAffinity: ClientIP
